# Fixtures

Reusable `.lcraft` files and expected export artifacts live here.

- `generic-minimal.lcraft`: 最小構成の汎用CAD fixture。読込、保存、schema 追従確認に使う。
- `interface/`: UI/Core 境界の代表JSON。意味コマンド、パーツコマンド、出力・印刷レスポンスのschema契約確認に使う。

レザークラフト固有の受け入れ検証 fixture は、汎用CADとしての基礎機能を優先するため active test set から外している。
