# AGENTS.md

このプロジェクトの文書は、まず [`docs/README.md`](docs/README.md) を読み、作業対象に応じて `docs/spec/` または `docs/design/` の正本を確認してください。`docs/old/` は過去資料の保管場所であり、通常の読み込み・更新対象ではありません。

## 文書の責務

- `docs/spec/` は利用者から観測できる機能、ファイル形式、UI / UX の外部仕様を記述します。
- `docs/design/` はアーキテクチャ、内部境界、機能領域ごとの概略設計を記述します。詳細実装の台帳にはしません。
- `docs/glossary.md` は複数文書で共通して使う用語の意味を記述します。
- `docs/backlog/` は未確定事項と将来候補を記述します。

`.lcraft`、JSON Schema、UI/Core の wire shape、製品挙動を変更しない文書再編では、相対リンクと文書内の責務を更新します。外部仕様には内部実装や一時的な検証記録を混ぜません。
